#include<stdio.h>
int main(){
    int a; //variable declaration
    int A;
    int _a;

    a = 5;
    A = 10;
    printf("a is %d and A is %d", a, A);

    //Rules:
    //First character must be an alphabet or underscore(_).
    //No commas, blanks allowed.
    //No special symbol other than (_) allowed.
    //Variable names are case sensitive.

    //Constants
    //An entity whose value does not change is called as a constant.
    //A variable is an entity whose value can be changes

    //Types of constants
    //1. Integer Constant : 1, 2, 6, 7, 9
    //2. Real Constant : 322.1, 1, 2.5, 7, 0
    //3. Character Constant : 'a', '$', '@' (must be enclosed within single inverted commas)

    

    return 0;
}